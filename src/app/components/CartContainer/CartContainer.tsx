import { CartItem } from '@/app/types/CartItem';
import React from 'react'
import { HorizontalCard } from '../Card/HorizontalCard';


export default function CartContainer({item, pathname}: {item: CartItem; pathname: string}) {

  return (
    // <div className=''>
    //   <HorizontalCard book={item.product} pathname={pathname}/>
    // </div>
  )
}
